{
  "name": "praxisprojekt-ss-2025",
  "version": "1.0.0",
  "description": "Praxisprojekt",
  "scripts": {
    "install-backend": "cd backend && npm install",
    "start-backend": "cd backend && npm start",
    "dev-backend": "cd backend && npm run dev",
    "postinstall": "npm run install-backend"
  },
  "devDependencies": {
    "prisma": "^6.10.1"
  }
}